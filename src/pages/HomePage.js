import React from 'react';
import { Typography, Button } from '@mui/material';

function HomePage() {
  return (
    <div>
      <Typography variant="h1">Welcome to ASC</Typography>
      <Button variant="contained" color="primary">
        Learn More
      </Button>
      // Add other sections as per the design
    </div>
  );
}

export default HomePage;
